DROP-IN SLOTS — Page background

hero-bg.jpg
  A Minecraft screenshot, landscape orientation.
  Recommended: 1920x1080 or larger, vista with depth.
  The CSS handles blur + dark overlay, so a raw screenshot is fine.

hero-bg-mobile.jpg  (optional)
  Portrait crop for phones, 1080x1920.
  If missing, the desktop image will be reused.

If neither exists, the page falls back to a solid dark color.
No code changes needed when you add the files.
